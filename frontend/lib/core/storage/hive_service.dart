/// Placeholder new-architecture Hive service for StudyFlow AI.
///
/// NOTE: This class is intentionally separate from the existing
/// HiveService classes in `notesApiServices/` and
/// `screens/dashboard/schedule/models/`. Those classes are preserved
/// and remain the authoritative Hive setup in Phase 1.
///
/// Phase 2: Consolidate Hive initialization here.
class AppHiveService {
  AppHiveService._();

  // Initialisation is handled by the existing HiveHelper classes
  // called from main.dart. No duplicate init here.
}
